steps: 

# #install
# - name: node:8.15.0
#   entrypoint: npm
#   args: ['install']

# #test
# - name: node:8.15.0
#   entrypoint: npm
#   args: ['set', 'CI=true', '&&react-scripts', 'test', '--env=jsdom']

steps:
- name: gcr.io/cloud-builders/gcloud
  entrypoint: 'bash'
  args: [ '-c', 'gcloud secrets versions access latest --secret=BRNKL_DEV_CREDS > echo BRNKL_DEV_CREDS' ]

# #build
# - name: node:8.15.0
#   entrypoint: npm
#   args: ['run', 'build']

